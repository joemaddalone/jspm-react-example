To install/launch:

```sh
npm install -g jspm live-server
jspm install
live-server
```
go to http://localhost:8080/build.html

To build:

```sh
jspm bundle-sfx --minify lib/main
```
go to http://localhost:8080/build.html
